.env
__pycache__/
*.pyc
node_modules/
*.backup
*.db
chroma/
chroma_db/
data/private/
backups/
*.log
.vscode/
.venv/
.cache/
data/
.env
.env.*
!.env.example

test-chatwoot.html

# Local document dumps
DOCUMENTOS/

# Local migration backups
_backup_langgraph_step1/
_backup_rag_docs_*/

# Claude Code / dev tooling (not project files)
.npmrc
package.json
package-lock.json

# Toolchains locales — nunca versionar (Node, etc.)
.tools/

# Respaldos manuales de DB (pg_dump) — no versionar
schema_pre_*.sql
facts_pre_*.sql

# QA generated reports — outputs, no versionar
reports/
